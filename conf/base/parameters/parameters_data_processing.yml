raw_prints:
  explode_column:
    event_data
  #Accepted values for `type`: `str`, `bool`, `int`, `long`, `float`, `double`,
  #  `bytearray`, `bytes`, `decimal`, `date`, `datetime`, `time`, `timestamp`, `none`.
  # If the `type` is not recognized, StringType will be returned.
  columns_to_cast:
    day: date
    position: int
    value_prop: string
    user_id: int
  col_to_filter_by: day
  

raw_taps:
  explode_column:
    event_data
  columns_to_cast:
    day: date
    position: int
    value_prop: string
    user_id: int
  col_to_filter_by: day