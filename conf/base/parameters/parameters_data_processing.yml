# Prints data frame
raw_prints:
  col_to_filter_by: day

intermediate_prints:
  explode_column:
    event_data
  columns_to_cast:
    position: int
    value_prop: string
    user_id: int
  columns_to_rename: 
  # old_name: new_name
    day: date
  date_col: date

primary_prints:
  date_col: date
  last_n_days: 7

# Taps data frame
raw_taps:
  col_to_filter_by: day

intermediate_taps:
  explode_column:
    event_data
  columns_to_cast:
    position: int
    value_prop: string
    user_id: int
  columns_to_rename: 
  # old_name: new_name
    day: date
  date_col: date

primary_taps:
  date_col: date
  last_n_days: 7

# Pays data frame
raw_pays:
  col_to_filter_by: pay_date

intermediate_pays:
  date_col: pay_date

primary_pays:
  date_col: pay_date
  last_n_days: 7

primary_windows_parameters:
  date_col: date
  last_n_days: 7

# Reduce Join
primary_join_parameters:
  prints_df:
    cols_to_join:
      - date
      - user_id
      - value_prop
    join_type: left
    cols_to_keep:
      - date
      - user_id
      - value_prop
  taps_df:
    cols_to_join:
      - date
      - user_id
      - value_prop
    join_type: left
    cols_to_keep:
      - date
      - user_id
      - value_prop
  pays_df:
    cols_to_join:
      - pay_date
      - user_id
      - value_prop
  how: left
