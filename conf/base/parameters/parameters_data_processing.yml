# Prints data frame
raw_prints:
  col_to_filter_by: day

intermediate_prints:
  explode_column:
    event_data
  columns_to_cast:
    position: int
    value_prop: string
    user_id: int
  # [old_name, new_name]
  date_col_name_change: [day, date]

# Taps data frame
raw_taps:
  col_to_filter_by: day

intermediate_taps:
  explode_column:
    event_data
  columns_to_cast:
    position: int
    value_prop: string
    user_id: int
  # [old_name, new_name]
  date_col_name_change: [day, date]


# Pays data frame
raw_pays:
  col_to_filter_by: pay_date
