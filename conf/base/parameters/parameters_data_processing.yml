# Prints data frame
raw_prints:
  col_to_filter_by: day

intermediate_prints:
  explode_column:
    event_data
  columns_to_cast:
    position: int
    value_prop: string
    user_id: int

# Taps data frame
raw_taps:
  col_to_filter_by: day

intermediate_taps:
  explode_column:
    event_data
  columns_to_cast:
    position: int
    value_prop: string
    user_id: int
